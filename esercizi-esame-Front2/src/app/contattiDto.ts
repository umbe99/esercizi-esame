import { Contatti } from './contatti';

export class ContattiDto{

   contatto: Contatti;
   lista: Contatti[];
   msg: string;
  /*
   constructor(_contatto: Contatti,
    _lista: Contatti[],
    _msg: string){

      this.contatto = _contatto;
      this.lista = _lista;
      this.msg = _msg;
    }
    */
}
